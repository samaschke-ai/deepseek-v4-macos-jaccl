# Retired oMLX / JACCL / DSpark integration

Historical source and receipts only, not a supported launch recipe. These experiments do not establish a working current Vision deployment. The old tests remain unchanged with their dependencies (MLX and a patched external oMLX cache). Do not use old launch examples as defaults. See the root README for the only supported configuration. The generic streaming benchmark in scripts is retained but is not the controlled quant-comparison harness.
